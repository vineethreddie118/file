
    // Applying the same trimming logic as in fnHandleBillingAddressChange
    const billingAddress1 = location.billingAddress1 || '';
    this.oCurrentProviderLocationInfo.billingAddress1Current = billingAddress1.substring(0, 25);
    this.oCurrentProviderLocationInfo.billingAddress1 = billingAddress1.substring(0, 25);
    this.oCurrentProviderLocationInfo.billingAddress2 = billingAddress1.length > 25 ? billingAddress1.substring(25) : location.billingAddress2;
