// Trim and set billing addresses
    if (location.billingAddress1.length > 25) {
        this.oCurrentProviderLocationInfo.billingAddress1Current = location.billingAddress1.substring(0, 24);
        this.oCurrentProviderLocationInfo.billingAddress2 = location.billingAddress1.substring(24);
    } else {
        this.oCurrentProviderLocationInfo.billingAddress1Current = location.billingAddress1;
        this.oCurrentProviderLocationInfo.billingAddress2 = location.billingAddress2;
    }
