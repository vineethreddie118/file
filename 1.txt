if (this.arrFilteredLocations) {
          this.arrFilteredLocations = this.arrFilteredLocations.map(location => {
            return {
              ...location,
              providerBillingInfo: location.providerBillingInfo || [] // Assuming providerBillingInfo is an array or object
            };
          });
        }
      }
